/**
 * HolaMundo.java
 * Programa para programa para hacer mostrar ejemplos de nombres de variables y explica porque algunos nos son válidos
 * vmaf - 08/10/2020
 */

public class PruebaIdentificadores {
    public static void main(String argumentos[]) {
    	int _alpha;
    	float FLOAT;
    	//int 1_de_muchos; // los subrayados deben ir entre dígitos.
    	char maxValor;
    	int cuantos;
    	//double "dado"; // el nombre de variable no puede ir entrecomillado.
    	int Nbytes;
    	//char pink.panter // el nombre variable no puede llevar un punto.
    	//double int // int es una palabra reservada y no puede ser usada como nombre de variable
    	//float qué_dices?; // no se puede poner ? en un nombre de variable
    	long Número;
    	//short cadena 2; // los números deben ir con subrayado
    	char Cañón;
    	int café;
    	//double Mesa-3; // los números deben ir con subrayado
    	byte Return;
    	char While;
    	int __if;
    	//double Bloque#4; // no se puede utilizar #
    	//int c o s a; //no se pueden utilizar espacios
    	char _CaPrIcHoSo_;
    	//short 8ªRoca; // no se puede utilizar ª
    	//int 3d2; // los números deben ir con subrayado
    	//float Hoja3/5; // No se puede utilizar /
    }
}